#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    double a, b, c, d;
    
    cin >> a >> b >> c >> d;
    
    if (a > b && b > c && c > d){
        // do something
    }
    
    return 0;
}
